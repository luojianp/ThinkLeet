
逻辑上动归都是DAG，DAG中的节点就是一个中间结果，边就是基于那个结果而可以进行变换的条件。
只不过DAG 分成了几种类型。

1 固定累积型（DAG相当于节点前驱肯定是和自己相关的固定序号的值）

例子：climbing stairs
公式eg : f[i] = f[i-1] + f[i - 2] 

2 单向遍历所有结果型（DAG相当于是前驱肯定比自己的序号要小）

例子：13.6.3 线段覆盖

公式eg : f[i] = max{f[j] + k} ( 0 < j < i)

3 双向夹逼组合所有结果型(DAG相当于每个点都有两个前驱，而且前驱的序列也是有关系的)

例子：13.7.1 最优矩阵链乘
公式 : d[i][j] = min{d[i][k] + d[k+1][j] + p(i -1)pkpj}
